require 'spec_helper'

descibe "HasMany" do
  describe "normal execution" do
    it "ignores tracking"
    it "allows saves"
  end

  describe "import execution" do
    it "imports models with relation set"

    it "blocks saves by default"
    it "ignores saves if option set"
  end
end
